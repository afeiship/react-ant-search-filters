{
  "name": "@jswork/ant-form-builder-widgets",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "start": "cd ./packages/sites && yarn start",
    "build": "lerna run build --since master",
    "publish": "npm run build && lerna publish"
  }
}